# AI游戏平台环境配置示例 - 需求 7.3
# 复制此文件为 .env 并根据您的环境进行配置

# =============================================================================
# 全局配置
# =============================================================================

# 环境类型: development, staging, production
ENVIRONMENT=development

# 调试模式
DEBUG=true

# 日志级别: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# =============================================================================
# 服务端口配置
# =============================================================================

# 撮合服务端口
MATCHMAKER_PORT=8000

# 游戏服务器工厂端口
FACTORY_PORT=8080

# 示例游戏服务器端口
EXAMPLE_SERVER_PORT=8081

# 动态游戏服务器基础端口（工厂创建的服务器从此端口开始分配）
BASE_PORT=8082

# =============================================================================
# 撮合服务配置
# =============================================================================

# 心跳超时时间（秒）
HEARTBEAT_TIMEOUT=30

# 清理间隔时间（秒）
CLEANUP_INTERVAL=10

# =============================================================================
# 游戏服务器工厂配置
# =============================================================================

# 最大文件上传大小（字节）- 默认1MB
MAX_FILE_SIZE=1048576

# 最大容器数量
MAX_CONTAINERS=50

# 容器内存限制
CONTAINER_MEMORY_LIMIT=512m

# 容器CPU限制
CONTAINER_CPU_LIMIT=1.0

# 闲置超时时间（秒）- 30分钟
IDLE_TIMEOUT_SECONDS=1800

# 资源清理间隔（秒）- 5分钟
CLEANUP_INTERVAL_SECONDS=300

# =============================================================================
# 示例游戏服务器配置
# =============================================================================

# 示例房间名称
EXAMPLE_ROOM_NAME=示例游戏房间

# 示例房间密码（留空表示无密码）
EXAMPLE_ROOM_PASSWORD=

# 示例房间最大玩家数
EXAMPLE_MAX_PLAYERS=20

# 心跳间隔（毫秒）
HEARTBEAT_INTERVAL=25000

# 重试间隔（毫秒）
RETRY_INTERVAL=5000

# =============================================================================
# 安全配置
# =============================================================================

# 允许的跨域来源（用逗号分隔，* 表示允许所有）
# 生产环境中应该设置具体的域名
ALLOWED_ORIGINS=*

# =============================================================================
# 客户端配置（用于Flutter应用编译时）
# =============================================================================

# 撮合服务URL
MATCHMAKER_URL=http://127.0.0.1:8000

# 游戏服务器工厂URL
GAME_SERVER_FACTORY_URL=http://127.0.0.1:8080

# 应用名称
APP_NAME=AI游戏平台

# 应用版本
APP_VERSION=1.0.0

# 房间刷新间隔（秒）
ROOM_REFRESH_INTERVAL=30

# 网络配置
MAX_RETRIES=3
RETRY_DELAY_MS=2000
CONNECTION_TIMEOUT_MS=30000
MAX_FILE_SIZE_BYTES=10485760

# 调试配置
ENABLE_DEBUG_LOGS=true
ENABLE_PERFORMANCE_MONITORING=false
SHOW_DETAILED_ERRORS=true
ERROR_DISPLAY_DURATION_MS=5000

# 缓存配置
CACHE_EXPIRATION_MS=300000

# =============================================================================
# 生产环境配置示例
# =============================================================================

# 取消注释以下配置用于生产环境
# ENVIRONMENT=production
# DEBUG=false
# LOG_LEVEL=WARNING
# ALLOWED_ORIGINS=https://yourdomain.com,https://app.yourdomain.com
# MATCHMAKER_URL=https://matchmaker.yourdomain.com
# GAME_SERVER_FACTORY_URL=https://factory.yourdomain.com
# MAX_CONTAINERS=200
# CONTAINER_MEMORY_LIMIT=1g
# CONTAINER_CPU_LIMIT=2.0
# ENABLE_DEBUG_LOGS=false
# ENABLE_PERFORMANCE_MONITORING=true
# SHOW_DETAILED_ERRORS=false

# =============================================================================
# Docker Compose 覆盖配置
# =============================================================================

# 如果您使用 docker-compose，以下变量将被自动使用
# 您也可以创建 docker-compose.override.yml 文件来覆盖特定配置