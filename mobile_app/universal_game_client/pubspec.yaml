name: universal_game_client
description: A universal game client for AI-generated games.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.1.1
  http: ^1.1.0
  webview_flutter: ^4.4.2
  permission_handler: ^11.0.1
  shared_preferences: ^2.2.2
  socket_io_client: ^2.0.2
  file_picker: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  glados: ^1.1.1

flutter:
  uses-material-design: true

  # 添加权限配置以便在Android上使用相机
  # android.permissions: ["CAMERA"]